<resources>
    <!-- Base application theme. -->
    <style name="Theme.Colorlink" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@color/black</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/flow_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.Colorlink</item>
    </style>
</resources>
